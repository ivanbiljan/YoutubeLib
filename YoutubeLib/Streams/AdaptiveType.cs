namespace YoutubeLib.Streams
{
    /// <summary>
    ///     Specifies the type of an adaptive video (either video-only or audio-only).
    /// </summary>
    public enum AdaptiveType
    {
        None,
        AudioOnly,
        VideoOnly
    }
}